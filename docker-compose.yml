services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    env_file:
      - path: .env
        required: false
    environment:
      NODE_ENV: production
      NITRO_HOST: 0.0.0.0
      PORT: 3000
    ports:
      - '3000:3000'
    restart: unless-stopped
